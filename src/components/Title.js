import React from 'react';
import './Title.css'; // assuming the CSS is in a separate file called Title.css

const Title = () => {
  return (
    <h1 className='title'>Patient AI Assistant</h1>
  );
};

export default Title;
